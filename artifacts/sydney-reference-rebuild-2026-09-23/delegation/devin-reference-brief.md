You are Devin SWE-2, explicitly authorized by user for a bounded coding task. WonderForge repo. Read AGENTS.md, specs/13 (latest owner reference reset), specs/49, HANDOFF.md first. Own ONLY new script scripts/verify-sydney-reference-views.mjs and artifacts/sydney-reference-rebuild-2026-09-23/delegation/devin-report.md. Do not change app source/specs/other scripts. No git operations, commits, installs, deployment or publishing. Other agents are changing model and geography concurrently.

Implement a reusable deterministic Playwright capture script for reference matching of Sydney. Read scripts/probe-sydney-background.mjs (isolated WorldScene module imports), scripts/verify-sydney-overhaul.mjs and camera/runtime interface. Use existing dependencies. Default dev URL http://127.0.0.1:5590 with optional SYDNEY_QA_URL and SYDNEY_QA_OUT env. Create isolated production WorldScene with Sydney at t=1 and same fixed daylight e.g. t=.58 lighting if possible (avoid dusk obscuring geometry). Capture the real production scene from N, S, E, W, high roof-plan, and north-west aerial angles. Coordinate convention X east, Z south. Provide full context frame plus tighter architectural frame for useful comparison. Target 1200x900 desktop, optional portrait. Await resources. Report camera positions, target, draw calls/triangles, page/console errors and asset bundle/import URL. Write an HTML contact sheet with filename captions and optional two local source images from ../references/user-aerial.png and user-side.png. DO NOT use substitute inspection geometry: must load actual production WorldScene and preserve renderer/lighting. Scene API may change: access available camera and update only after normal update. This is architecture acceptance tooling only, not changes to production camera. Run script against existing dev server into new output artifacts/sydney-reference-rebuild-2026-09-23/baseline-views to validate useful captures. Inspect one output if possible. Do not declare current model acceptable. Return exact model/commands/files and limitations in owned report.
