import{sydneyCinematicShotAt}from'../../src/engine/sydneyCamera';import{Vector3}from'three';for(const aspect of[1.6,390/844]){let max=0,at=0,prev:Vector3|undefined;for(let i=0;i<=3600;i++){const s=sydneyCinematicShotAt(i/3600,aspect),p=new Vector3(s.target[0]+Math.cos(s.azimuth)*Math.cos(s.pitch)*s.radius,s.target[1]+Math.sin(s.pitch)*s.radius,s.target[2]+Math.sin(s.azimuth)*Math.cos(s.pitch)*s.radius);if(prev&&p.distanceTo(prev)>max){max=p.distanceTo(prev);at=i/3600;}prev=p;}console.log({aspect,max,at});}
