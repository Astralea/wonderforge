Pass 139 **delays far-pylon (ne/nw) mid-leg until after t = 0.32** so the
join postcard is two tall Seine legs plus shorter far feet, not a nested
second A. Far mid uses 0.55× span so it still finishes before the shaft
(~0.48). An uncompressed far span delayed the shaft and emptied the BUILD
needle (dark 0.757 → 0.341); that span is retired.

pngjs vs 138: join-upper dark **0.121 → 0.097** (lost nested far A);
join-gap flat; join-left/right dark stable-to-up; BUILD needle **0.757**
unchanged. Opening unchanged.
