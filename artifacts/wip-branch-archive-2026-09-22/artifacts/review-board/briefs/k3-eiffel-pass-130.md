# K3 Visual Director — Eiffel Tower passes 129–130 (negative)

**Model:** `kimi-k3-max` (Factory Droid / `devin --model kimi-k3-max`)
**Scene:** `#/debug/wonder/eiffel-tower/<t>` typed `eiffel-tower-reference` (Spec 14).
**ID:** `eiffel-tower` unchanged.
**Live bundle / pixels:** pass 128 (`index-_yluHPG1.js`, `artifacts/eiffel-pass-128/`).
**Negative evidence:** `artifacts/eiffel-pass-129/` (trapezoid, reverted) and
`artifacts/eiffel-pass-130/` (mid-leg Seine horizontals, reverted).

Giza is the quality bar. These two probes failed and were reverted. Score
the **live pass-128** captures, not the reverted frames:

1. A **ground trapezoid** (near ~76 m / far ~128 m) cannot un-nest four
   feet from due north because the join silhouette is the **square P1
   meeting**. Join-gap was flat; BUILD cream rose. Do not restore it.
2. **Mid-leg Seine storey horizontals** at ~318 m are silhouette noise
   (north-X dark 0.505 → 0.506), same family as pass 102's lower mid-bay
   rail. Do not restore them.

Score t=0.12 Palais drums + four-leg ticks, t=0.32 four feet vs two
arches, t=0.58 A-plus-needle vs a brown stick, t=0.78 campanile. Cite
PNG/JSON. In-session coding-model screenshots are not sign-off.

Do not restore a ground trapezoid. Do not restore mid-leg or lower Seine
mid-bay horizontals. Do not restore 380/460 or 468/528 m BUILD radii, a
0.9/0.06 lantern, a 124 m dusk look, 22° join yaw, 28 m look offset,
z ≈ 50/78, Champ-spanning gold jibs, or the rest of the pass-128 burned
list.
