# Pure luff reeving

`solveEiffelSummitLuff` returns the five joined yaw-local line/arc segments, rendered sample points, exact deployed length, moving-block positive-pin position and reference drum angle. It accepts the actual boom pitch, optional reference pitch and an explicit optional total inventory. It never invents stored rope when inventory is omitted.

The initially proposed top guideX+.12 crossed a zero/full-wrap branch during the boom sweep. The accepted guideX−.20 keeps its clockwise wrap between9.91° and105.46° across0–90°. The moving block retains its0.15m connection offset from the boom pin. Deployed length ranges9.1379134–12.1327116m; a fixed.06m reference drum takes up7.94395 turns across the full sweep. Five tests passed for tangent derivatives, exact join coordinates, arc radii/length, continuous wrap, reversible drum balance and explicit inventory errors.

This is not a demonstrated physical winding: drum width, fleet contact, storage layers and the handchain worm drive remain separate gates. The becket endpoint is its ring-centre attachment datum; a visible eye/knot termination must establish real source contact. Actual source luff-rope clearance and complete cargo operation are not admitted by these pure tests. The source moving block already has pairedZ±.05 cheek supports outside its groove plane.
