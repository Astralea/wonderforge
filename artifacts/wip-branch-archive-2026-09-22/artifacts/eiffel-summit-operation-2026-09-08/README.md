# Summit operation candidate — 2026-09-08

This pass replaces the rejected mirrored lower cargo frame with an actual
Blender model that clears the ladder. It adds the revised annular cargo feed,
a sliding keyed drum, explicit winding, a grooved traverse barrel, and a
separate two-fall luff tackle. The parent built and read back the editable
source through the installed Blender Lab MCP after a read-only desktop probe.
No plugin was installed.

Current source is `blender/eiffel-summit-operation-rig.blend`, with a separate
contextual review scene beside it. The V4 GLB is
`model/summit-operation-rig.glb`, SHA256
`a5a2a96ede7dd1d1f8b37573e85ba6118bee0226a8ad2bc7ea552eee194a6d82`.
It contains1099 objects,1021 mesh objects and78 named roles. Older V1–V3 assets
and build/readback evidence are retained under `versions/`. The original
V6 cargo artifact, V9 gin-pole source and public tower/city assets are retained.

The local review is `preview/index.html` on the existing5590 development
server. It has playback, speed and orbit controls. Its319-second mechanism
clock contains two112-second lifts, the70-second pole climb and a25-second
empty-tackle return. The separate80-second worker-access review is
`access/preview/index.html`. Long spans keep a depth-tested one-pixel display line, preserving their physical
meshes and routes while preventing broken subpixel spans in the overview.
These are candidates, absent from the production
film at5589. The latter still uses the previously shipped Paris/water revision.

## Evidence and limits

- `mcp/readonly-9876.json` is the successful read-only desktop probe. Build and
  final source readback responses are retained; the parent's temporary
  Blender24471 was stopped after readback. Original desktop62427 was preserved.
- `engine/source-sweeps.json`:449 actual crosspin/chord poses and201 actual
  cart/assembly-one poses are clear, with the stated contact tolerance and
  bounded geometry scope. This does not cover every rig body or assembly two.
- `engine/cam-contact.json`:193 phases and1737 actual groove/follower lower-face
  rays are clear. The minimum sampled clearance is0.2287mm. This is not proof
  of positive branch selection, side contact, strength or continuous steering.
- `access/README.md`:161 actual worker poses clear; final V4 desktop/mobile
  source hashes,20-query reviews and reversal evidence. Custom-scene counts
  do not establish the production film's mobile budget.
- `rope/README.md`: conserved120m inventory, current-radius tangents, fixed
  bridles, complete winding and exact shaft phase, with independent sampled
  spacing/integration checks. Its original metrics retain their source hashes.
- `luff/README.md`: continuous tangent wraps and explicit analytical inventory;
  this does not admit luff winding, termination or visible operation.
- `final-tests-after-distance-lines.log`, `typecheck-final.log`, `build.log` and `preview/qa.json` record the
  completed checks for this pass; see `final-verification.json` for final status.

The principal remaining work is ground-to-terrace material provenance, second
stock arrival/attachment, visible operation of guides and winches, receiving
sling removal, complete rope/body clearance and production integration. The
present shallow-angle diamond-cam crossings do not demonstrate retained branch
selection. The reduced-cam design note is a possible next revision, not a
certified historical mechanism. This reconstruction is interpretive; neither
strength nor the exact Eiffel apparatus is certified.

The goal remains ACTIVE. Do not describe this candidate as a completed repair
of the production animation or proceed to another wonder on that basis.
