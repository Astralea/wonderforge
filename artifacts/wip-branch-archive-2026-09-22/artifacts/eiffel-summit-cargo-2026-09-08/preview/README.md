# V6 isolated cargo preview

The renderer remains outside EiffelWorld and the production film. Actual seated terrace/c000 context, visible analytical rope/bridle, positioned block and opposed tail pins are included. Full-capacity reference winding is hidden. Both assemblies retain exact member IDs and source poses.

Final source SHA256: 17db309645e75584789db5757e4ac0e75abb8135df93c32ef30164548c2e8d46. Desktop1440x900 and mobile390x844 each passed17 settled assembly1 seeks plus6 assembly2 seeks. Every seek waits for the rendered sample time; errors0. The18→63→18 return is pixel-identical. `qa-v6-unsettled.json` preserves the rejected short-delay harness output, whose labels sometimes lagged the requested time; it is not final evidence.

`qa.json` records actual renderer counts and1405static context IDs separately from cargo IDs. The main README and final-verification.json record physical scope: source cart and head sweeps pass, but the cargo upper fairlead obstructs the ladder. Full operational admission, winding and luff controls remain incomplete.
