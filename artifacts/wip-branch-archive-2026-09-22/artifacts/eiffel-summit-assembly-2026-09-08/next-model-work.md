# Next work: replace the remaining unsupported summit equipment

The accepted production capture
`browser/production-desktop/final-wave-end-minus-3.6.png` is the concrete next
defect: a large generic crane and its outboard pad remain beside the slender
flagstaff. The cargo now travels as one assembly, but this picture still fails
the intended mechanical appearance. Do not call the whole scene realistic from
the green route or browser gates.

Use the parent-owned Blender candidate, `support-audit/climb-plan.md` and
`support-audit/climb-implementation-plan.md` to build the actual supported
replacement. First add a real collar-to-tackle reaction member, a ratcheting
drive, opening guide frames with pins, and a clear operating-chain path to a
supported worker station. Then implement the continuous c001-to-c002 climb and
replace the two generic rigs together. The current candidate only proves static
geometry; it is not ready to be connected by moving its whole root upward.

Keep the visible material supply/handoff in this same scope. Starting a piece
in elevated stock is still an omitted operation, even when its later path is
clear. Either show the actual supported relay to that stock or explicitly retain
an honest omission cut; never claim complete ground-to-top construction.

The production c000 chapter is also too distant for contact review: the actual
normal camera at578–582s shows the whole tower, and the receiver occupies only a
few pixels. A diagnostic close camera can inspect geometry, but does not improve
the user's film. Integrate a coherent close framing/pacing for all three mast
assemblies when the real supported rig and handoff are ready, preserving the
monotone orbit and readable final seating. Do not add another isolated camera
recertification pass while that larger mechanism remains missing.

Paris architectural subpixel filtering is complete within its measured scope.
Water remains a distinct issue. Keep it as the next rendering task after the
mechanical replacement; do not rerun the existing A/B unchanged. No next wonder
until these major Eiffel defects are resolved.
