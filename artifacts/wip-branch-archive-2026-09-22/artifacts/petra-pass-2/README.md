# Petra pass-2 QA

Debug route: `#/debug/wonder/petra/<t>`
Cinematic: `#/wonder/petra`
Bundle: `index-BBKG7rxX.js`
Diagnostics: `petra-reference` on every captured frame, no console/page errors.

Desktop 1440-class captures at t = 0.12, 0.32, 0.58, 0.78, 0.92, 1.0 plus
one mobile 390×844 frame at 0.58 and a cinematic open.

Pass-1 (`artifacts/petra-pass-1/`) used a wider azimuth and parked the
reveal camera inside the east Siq wall. Pass-2 keeps the lens in the gorge
corridor. Remaining review notes: dark sky slot, blocky envelope, and
unscored audio.
