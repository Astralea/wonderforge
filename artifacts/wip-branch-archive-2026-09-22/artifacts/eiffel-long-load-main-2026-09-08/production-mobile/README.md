# Final mobile production verification — 2026-09-08

Production `http://127.0.0.1:5589`, Chromium mobile emulation390×844,DPR1.35.
Served bundle `/assets/main-SiNzKaoE.js` hash83aaad4e6345c31ae102796e8c2571cf461597419828a20316986e7ae7d84ab6.
The browser received byte-verified Paris city, V4 steam drive and driven receiver assets; hashes are in report.json.

101 full-film seeks across399seconds plus9actual delivery chapter poses passed. Peak298410triangles/149calls stays below300000/150. No browser errors or horizontal overflow. Actual Space key play/pause advances. Reverse116→124→116seconds returns a pixel-identical central scene crop.

The first production run failed its triangle budget:362349peak, early activechapter~303k. Evidence is retained in ../production-mobile-before-culling/. The final code enables per-instance frustum culling on both exact-geometry equipment batches and hides the Eiffel world only beneath fully opaque film cuts, while all state continues advancing. No geometry or asset bytes were changed. All9before/after chapter scene crops are pixel-identical; culling-visible-comparison.json records this comparison. Early chapter now281475triangles.

Ground carrier and124second landing captures were visually inspected. Machinery and operator are present; tower cross-bracing partly occludes the landing machinery in the portrait orbit. This is a sampled rendering/interaction gate, not a claim that all city flicker or physical-construction omissions are solved. Hook remains attached at chapter end; later relays and final installation are still represented by an explicit omitted-work cut.

Verification chain: prior fullsuite757/130passed; final engine+renderer integration22/22passed after a timeout-only test budget correction; culling renderer suites6/6passed; real EiffelWorld cut-visibility/update test1/1passed; final typecheck/build passed. Parent separately owns final desktop QA.
