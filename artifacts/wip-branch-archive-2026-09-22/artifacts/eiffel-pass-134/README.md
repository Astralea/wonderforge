Pass 134 raised the join look from **40 m to 72 m** so the camera would
frame the P2-height A instead of its ankles.

**Result:** join-gap cream **0.002 → 0.201**, join-left dark **0.483 →
0.198**, join-center dark **0.321 → 0.167**. South-Champ cream through the
open portal — the same overshoot as z ≈ 78. Reverted to 40 m. Do not
restore a **72 m** join look.
