# Main renderer integration

`EiffelLongLoadFilmSystem.updateRelay(sample)` continues the existing loaded carrier, stair, retained sling and saddle after the original 280-second first-floor operation. No second payload is instantiated and no final seated state is invented. The first-floor cart and original hoist stay behind. The new upper cart and prepared receiving equipment belong to `EiffelSecondFloorRelaySystem`.

`EiffelWorld` now dispatches the combined 348-second source sequence at two source seconds per film second. Main duration is829.4267707038563 seconds. Reverse seeking first restores the original operation state, then applies the deterministic continuation, including bolt/tool/worker poses, the retained saddle and both rope owners. The sole payload identity remains suppressed from the static kit until the opaque later-work card ends.

Spec32 and `tests/eiffel-second-floor-relay-main.test.ts` define the integration contract. See `../main/` and `../main-gpu/` for final source and browser evidence. Arrival at the upper cart remains separate from the omitted 197 m relay, carrier extraction and final installation.
