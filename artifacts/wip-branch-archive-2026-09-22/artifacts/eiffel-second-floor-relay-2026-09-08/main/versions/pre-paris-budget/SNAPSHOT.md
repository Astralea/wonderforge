# Pre-Paris-budget snapshot

The authoritative identity in this snapshot is `build-identity.json`: `main-CbeRpit_.js`, 2,103,885 bytes, SHA256 `bad7a5a8052521538989dcfde5137b5a6fa1988fc8dd9bd91bf98abd39e8326f`. It includes the final 4-second landed hold and 280-degree, 45-degree-pitch contact detail. Actual local HTML, bundle and relay response bytes matched dist/public.

The copied README and checks-status files record an earlier documentation state and refer to the preceding build. They are preserved unchanged as historical files; use this note and build identity for this snapshot.

This build's mobile final-wide frame measured 312,542 triangles, exceeding the unchanged 300,000 cap. It is a preserved candidate, not final budget admission. The complete 953-test final-hold baseline subsequently passed in the parent directory's `test-final-hold.log`.
