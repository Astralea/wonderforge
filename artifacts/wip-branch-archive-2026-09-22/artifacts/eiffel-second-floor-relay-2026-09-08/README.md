# Eiffel first-to-second-floor relay — 2026-09-08

This pass carries the same named stair member and carrier from the already shown first-floor arrival through a visible second-hook connection, removal and storage of four original cart bolts, a second lift, trolley traverse and landing on a separate second-floor cart. It is integrated into the main Eiffel film. The original 280-second first-floor mechanism remains unchanged; the new 348-second source sequence plays at 2× in the main film, plus a four-second landed hold, extending the movie to 829.4267707038563 seconds.

## Editable Blender source

Parent authored, saved and read back `blender/eiffel-second-floor-relay.blend` through the installed Blender Lab MCP after a successful read-only probe. V6 contains 322 objects, 301 mesh objects and 94 role entries. The exported `model/second-floor-relay.glb` and public `/models/eiffel-second-floor-relay/relay.glb` have SHA256 `4b71e83c5e2403feceb44e6da7543fcc3ac96736af969adabdd96807067f4e76`. Versions 1–5 and their evidence are preserved. Temporary Blender 56731 was stopped after readback; user desktop Blender 62427 remains running. No plugin was installed.

Build scripts: `scripts/blender_eiffel_second_floor_relay.py` and `scripts/build-eiffel-second-floor-relay-via-mcp.py`. Asset dimensions, roles and deliberate limits are in `asset-design.json`; actual MCP receipts are in `mcp/`.

## Verification and review

`route/` retains the original sealed V4 actual-source geometry and rope checks. `main/source-comparison.json` compares V4 to actual public V6: all 165 upper meshes retain exact world-triangle hashes. The 15 modified lower meshes are the tray/tool translation and six members on the three lower scaffold rows. `handoff/` contains the actual V6 worker, bolt and support checks. `main/` contains integration, test and build evidence; `main-gpu/` contains actual desktop/mobile rendering evidence and rejected camera-angle studies. Final production visual status is recorded in `final-verification.json`. Production Chromium viewport checks pass: desktop343,524 triangles/118calls, mobile293,212/106, zero errors, exact reverse screenshots and working Play/Pause. The original limits remain unchanged. Fine wrench/bolt/tray details are not fully resolved through the lattice; mobile here means a Chromium viewport, not a physical-device frame-rate claim. Requested screenshot times follow the normal0.001 seek step; exact subframe joins belong to engine tests.

Local main preview: http://127.0.0.1:5589/?review=second-floor-relay-v6-final#/wonder/eiffel-tower
Diagnostic mechanism preview: http://127.0.0.1:5590/artifacts/eiffel-second-floor-relay-2026-09-08/preview/

## Limits and next work

The goal remains active. This is an illustrated, geometrically checked operation, not a structural capacity or complete mechanical admission. Prepared equipment erection, the upper rope approach, drum inventory/braking/traction details and fine hand/tool contacts remain limited. The visible cart arrival is not final installation: the 197 m relay, carrier extraction and final tower installation remain explicitly omitted behind a later-work card. The separate summit-operation candidate has not yet been connected to this chain. Do not move to another wonder or mark the overall Eiffel goal complete on this pass alone.

No new Paris photogrammetry or city geometry was created in this pass. Exact source city triangles, tree instances and excavated terrain were partitioned for offscreen culling; source-preservation tests pass and no population or detail was removed. The existing Paris background is retained; older city/sky captures do not substitute for the new main-film checks.
