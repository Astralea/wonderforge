# Bounded upper freight engine candidate

The pure receiver and197m freight modules pass15 focused tests across2 files; typecheck passes. `verification.json` records exact source, fixture and test hashes. No production clock, renderer, public model, Blender script or saved Blender scene was changed by this subtask.

`sampleEiffelUpperFreight(seconds,start,design,pickup?)` requires an explicit starting witness and consumes the existing `freight-design.json` keys. `solveEiffelUpperFreightRope(carrierOrigin,design)` exposes analytic segments, tangents, exact deployed length, renderer points and source guide names. The core clock is106s lift,16s bridge traverse,4s lowering and4s landed hold. An explicit optional pickup adds its supplied over-guard lift and lane-change durations. The lower datum is never defaulted to an unsupported118.2m position.

Four real quarter-circle sheaves guide the.008m rope. The actual.242m groove plus rope gives.25m centreline radius; the actual.292m drum barrel plus rope gives.30m takeoff radius. The drum's takeoff uses axialZ−4.40, which the actual mesh confirms lies within its barrel. The moving feed span stays positive, from2.10m atbridgeZ−1.8 to.30m at−3.6. For the source datums, exact deployed length is219.3032960216191+bridgeZ−carrierY. The1.8m traverse therefore reels in1.8m even at constant load height. Returned kinematic angles are a geometry-level rolling relationship; they do not admit the engine's winding or travelling drive.

The seven freight tests independently read actual exported guide centres, groove vertices/planes, drum barrel and separate `upper-197-clevis`/pin hierarchy, as well as all four actual carrier shoe corners. They also exercise positive tangent spans, exact arc length, unchanged named payload, rigid offsets, terminal support state, continuous/reversible optional pickup and rejection of conflicting support/bolt/connector witnesses. Their lower-cart support contacts and optional121m clearance input are deliberately synthetic unit fixtures, labelled in the test. Passing those input checks is not evidence that the missing cartbridge/attachment operation exists. Every returned candidate remains `geometryAdmitted:false` and `productionReady:false`.

The new source's `upper-197-rest-rope` is an editable rest model. A preview must replace that whole role with the sampled rope; drawing both would leave duplicate rope in the scene. A renderer must preserve source hierarchy/axis rotations, especially the drum's authored local axis. The bridge/cross-trolley role positions are local to their original parents. Kinematic angle axes are worldZ for drum/feed/head and worldY for horizontal corners; the bridge wheels roll aboutX. No renderer integration is included here.

The current production relay still ends atcarrier[-15,116.47999938964844,-1.8]. The upper receiver110s overlay ends secured with its second hook attached. It cannot be concatenated directly with freight: supported access/release, actual cartbridge movement, four upper-bolt removal/storage and attachment of the distinct new hoist remain required in between. The old first-floor cart/fasteners/rope and old second-hoist connector keep their own identities. Freight ends on the197m receiver with its new hoist connected; carrier extraction, temporary hardware removal and final tower fastening remain separate work.

The receiver worker's earlier endpoint workaround is preserved in `../../receiver/versions/south-foot-first/`. Parent moved the matched upper steam engine/drum in `second-floor-clearance.glb`; the current worker then uses a coherent north station throughout, without final-step tricks. Independent actual-source review by Maxwell reports239 clear poses, including dense101–110, with body, grip and support checks. That separate artifact is `../../receiver/geometry-audit.json`; this engine report does not recertify its browser or collision evidence.
