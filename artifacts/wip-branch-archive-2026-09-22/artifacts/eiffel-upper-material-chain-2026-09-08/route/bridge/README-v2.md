# Bridge v2 actual topology gate

Actual GLB SHA256`309d57edb5017f756ca55ef2b17c683f2147cdd072d86f3c0f47674b2ee984c9`;189 glTF mesh primitives. The parent's187 Blender-mesh count can differ after material splitting during export.

Actual transformed triangle-versus-triangle checks find zero proper surface crossings with the frozen tower/current relay/197m platform. This tests the Boolean notch topology rather than filling its empty holes with mesh bounding boxes. The same triangle gate reproduces all seven old v1 collisions (`v1-triangle-control.json`), so the new result does not come from an insensitive replacement check. Sixteen bearing-sole corners retain actual ring-deck contact within2.135micrometres.

Files: `audit-v2.json/log/mjs`, `triangles.mjs`, `v1-triangle-control.json/log`. V1 remains preserved. Touching coplanar surfaces are treated as contact; this proper-surface-crossing gate does not by itself prove arbitrary complete-solid containment or load capacity. Vehicle ramp pitch, new steering fixtures, truss internal connections and crew motion still need separate actual-source checks.
