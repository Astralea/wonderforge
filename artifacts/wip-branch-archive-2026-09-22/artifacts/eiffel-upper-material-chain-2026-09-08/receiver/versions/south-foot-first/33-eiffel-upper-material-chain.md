# Eiffel upper material chain — draft

The shipped film ends its visible material chain with the same `summit-access-stair-m000-c000` and retained 6.317500305 m carrier landed on the second-floor receiving cart. The exact relay endpoint is carrier `[-15,116.47999938964844,-1.8]`, master `[-15,123.36249969482422,-1.8]`, with cart origin `[-15,116.13999938964844,-1.8]`. Arrival is not final installation. The subsequent 197 m transfer, carrier extraction and final tower seating remain required work; this specification does not claim that they exist already.

## First bounded continuation: secure the upper cart, then release the second hoist

Keep the current payload/carrier/master identity and pose throughout this stationary receiving operation. Retain the first-floor cart, worker, four removed fasteners, tools and first hoist at their existing terminal states. Four **new** upper-cart fastener identities (`upper-cart-bolt-0` through `upper-cart-bolt-3`) begin supported on a separately authored upper tray. They are never aliases for the old `cart-bolt-*` objects. A distinct upper receiving worker and wrench remain present from the prepared state.

Each new fastener must be picked up, carried with a matching hand, aligned above its actual upper-cart hole, inserted vertically and tightened through four complete turns. A hand, tray or engaged cart nut must own every fastener throughout. No lateral motion is allowed after the shaft enters the shoe. The carrier stays on the upper-cart bed and the hoist stays connected until all four fasteners are fully inserted and tightened. The worker then walks to actual supported access, climbs, transfers the retained master onto its existing carrier saddle by unloading the second connector, opens the keeper, withdraws the pin and leaves the released connector with its own rope. Opening the keeper or pin while the upper cart is unsecured is invalid. No source geometry is scaled and no worker or fastener teleports.

## Pure API and source contract

The new `src/engine/eiffelUpperReceiver.ts` owns only an upper-receiver state and role overlay. It must not alter the production clock, camera, old sampler, Blender source or renderer in this pass. `sampleEiffelUpperReceiver(seconds, design)` consumes explicit source datums and returns the invariant landed poses, four new bolt states, upper worker/tool role poses, upper-cart fastening/security state and second-hoist connector state. Old first-floor role poses are intentionally absent from this overlay; the caller preserves its existing terminal sample.

The design must identify the actual source revision and provide the rigid mapping from the verified first-floor handoff reference to the new receiver, the four seated and tray bolt centres, wrench holder/grip, access support and connector/saddle datums. A rigidly translated/reversed handoff curve is a candidate kinematic reuse, not proof that the new upper-floor route is clear. Parent-authored model readback must agree with these datums. Missing or conflicting source datums must fail or remain explicitly unadmitted; they must not be silently replaced by guessed contact coordinates.

The candidate fastening curve may reverse the existing handoff source seconds222→112. This reverses actual storage/carry/withdrawal motions into pickup/carry/insertion and preserves the same four full turns, with new role identities. The following source112→80 walk and80→66 climb may be reused only under the same verified rigid mapping. Reversing connector work requires explicit physical rope/unloading and retained-saddle support witnesses; a scalar named `slack` alone is not geometric evidence that a taut line has unloaded.

The initial standalone implementation exposes `eiffelUpperReceiverDesignAtYaw`, `transformEiffelUpperReceiverPoint`, and `sampleEiffelUpperReceiverReferenceWorker` for auditable yaw/translation alternatives. Every one of the eighteen original rigid worker body parts receives the same isometry; limb dimensions are not changed. The actual upper-cart station remains fixed. Non-cardinal work-path rotations are useful collision candidates, but cannot pass the release requirements gate unless the final bolts still match the four actual cart bores.

Actual geometry review selected yawπ as a layout candidate. Its original rigid walk still crossed a frame shoe, so it is not admitted merely because the central torso and feet cleared. The first local tool-only detour was rejected because dense body/fixture checks found cart, spreader and tray intersections, as well as a separate repeated bolt-carry crossing. The coherent second candidate relocates the new tray to `[-14.60,F2+.29,-2.555]`, its wrench to `[-14.60,F2+.32,-2.64]`, and the worker station to `[-14.20,F2,-2.555]`. Every tool/bolt transfer uses one route through centres `[-14.3,-2.05]`, `[-14.3,-1.50]`, `[-14.3,-.92]`, `[-14.65,-.92]`, `[-15,-.92]`, then the cart-side station `[-15,-1.13]`. Two alternating footfalls connect each neighbouring stance; the fixed-length connected-body solver recomputes each pose. Hands and carried shafts follow those solved targets, rather than moving the complete body and its planted foot together.

The wrench hand touches the shank's upper face: the local palm-centre datum is `[.12,.046,0]`, from the actual .006m shank half-height plus the .040m hand half-height. This avoids embedding the palm through the wrench and tray. Contact is blended at pickup and stow; the tool itself retains its source holder height.

The V2 actual-source sweep cleared the new recurring detours, core body and grip contacts, but found one16.284mm shin intersection at the final stationary pose against the south frame shoe (`X[-14.49588,-13.62590]`, `Z[-3.25644,-2.66843]`). Moving the complete working stance north was rejected: at upper106–106.5 the resulting crouch intersected the steam cylinder/back cap by up to28.784mm. The final bounded correction therefore preserves the clear working stance through upper108 and moves only the final footfalls: the south foot1 steps north by.15m during108–109, then north foot0 during109–110. This order lets the leg beside the obstacle leave first; the opposite order produced an actual triangle intersection at upper109. The final mean is `[-14.2,F2,-2.405]`, with feetZ `[-2.285,-2.525]`; tray, wrench, hand targets and all preceding carry routes remain unchanged. The actual .23m foot length gives a nominal south extent−2.64, beyond the shoe's north face. One foot stays planted while the other steps, and fixed-length IK recomputes the connected body. Dense actual posed-mesh and support checks across105–110 remain required before calling the final correction clear.

Because the tray station has moved, the reference-worker audit helper is now explicitly bounded to source112–222 (the110-second fastening section). It rejects earlier source times. The old walk/access cannot be silently joined to the new station; the next supported access must begin from the new final feet and hands. Browser slider quantization at0.001 will be a later integration concern, separate from exact engine join tests; no browser claim is made for this standalone module.

`sampleEiffelUpperReceiver` currently implements the110-second fastening/tool-return portion. Inputs beyond110 seconds hold the explicit `secured-awaiting-access` state with the second rope, keeper and pin unchanged. This is a deliberate boundary of the unintegrated candidate, not a completed release animation. `inspectEiffelUpperReceiverRelease` rejects missing source identity, false bolt-security flags, unaligned holes, missing saddle contact, unmoved rope endpoints, unsupported keeper/pin event ordering and absent required hand-contact points. Passing this data gate never sets `geometryAdmitted` true; actual supported worker/access geometry remains required. A yaw-rotated worker reference cannot silently rotate the existing world-Z clevis around the retained master.

## Verification and admission limits

Tests must establish deterministic forward/reverse sampling, all phase-join continuity, one planted foot during walking, connected fixed-length worker IK, bolt holder/hand contact, vertical insertion, four-turn tightening, unique old/new identities, invariant landed payload/carrier poses and the ordering of secure/unload/keeper/pin events. Actual exported cart holes, bolt/tray contacts, supported access, posed worker/solid clearance and rope/connector geometry require separate source and renderer checks before production integration. Reusing an old trajectory does not inherit clearance at a new height/location.

The complete upper chain additionally requires a supported 197 m receiver and load route, causal equipment arrival/erection, continuing material identity, extraction from the carrier, final assembly fastening, and removal or retained support of every temporary sling/fixture. These remain explicit future stages. This draft offers no strength, torque, rope inventory, historic reconstruction or whole-chain physical certification.
