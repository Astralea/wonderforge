# Continuous first-to-second-floor main-film relay

The existing long-load insertion continues after its unchanged 280-second first-floor sequence. It carries the same `summit-access-stair-m000-c000`, carrier, retained sling and saddle through the separately authored connector/bolt handoff and second-floor route. The continuation samples `sampleEiffelSecondFloorRelaySequence` at two mechanism seconds per film second. Its visible chapter duration is `280 + EIFFEL_SECOND_FLOOR_RELAY_SEQUENCE_DURATION / 2 + 4`; the final four film seconds hold the landed carrier on the upper cart before the opaque omission card so contact can be observed; the original first-floor sampler duration remains 280.

The production kit suppresses this exact payload identity throughout preparation, the complete extended chapter and the opaque later-work card. Arrival on the second-floor cart is not final tower installation. Only after the opaque omission card may the kit seat the member. The later-work text now omits the 197 m relay, extraction from the carrier and final installation. The preparation card identifies the preplaced upper receiving frame, access scaffold and tools, and discloses the omitted equipment erection, initial sling attachment and upper-rope approach.

`EiffelWorld` owns one `EiffelSecondFloorRelaySystem` loading `/models/eiffel-second-floor-relay/relay.glb`; its ready/dispose lifecycle belongs to the world. Its prepared hardware and separate upper rope are visible throughout the long-load insertion, beneath the existing opaque cuts. `prepareRelayRope` preplaces that line without moving the payload or first-floor crew. Through 280 seconds the original long-load and first steam-drive samplers keep their accepted clocks. Thereafter the original system first restores its 280-second state, then applies the combined continuation state to the same payload/carrier owner. The first hoist and first-floor cart remain at their actual terminal poses. Reverse seeks must restore the correct prepared/active relay state and old mechanism state without leaving stale rope or crew poses.

The existing camera is retained through 280 seconds. The continuation dispatches the separately owned `eiffelSecondFloorRelayFilmShotAt(continuationSeconds, aspect, azimuth)`. The first two continuation film seconds blend from the old 280-second shot to the new shot using a smooth cubic weight. Camera target, pitch, radius and field of view blend continuously; azimuth remains the shared monotone film orbit.

The orbit key at long-load start+280 stays exactly 95 degrees with slope0.03 degrees/second, preserving the accepted first-floor camera path. The connection closeup stays near96 degrees through continuation source second66. During the following descent/walk the orbit advances to199 degrees by source second116, then200 degrees at the start of the lift (source222),201 degrees at source300,280 degrees at source342 and281 degrees at chapter end. These work views look through the measured clear western gap between the tower piers. The completed film ends at287 degrees after beginning at−20, extending the arc to307 degrees. All new endpoint slopes remain0.03 degrees/film second. The visible orbit must remain strictly increasing for the extended clock.

The upper frame is established through source second336. During the final traverse, source336–342, the camera moves into a cart detail at26 m radius,45 degree pitch and a lower target aroundY117.2. Actual triangle sightlines choose the southern280 degree view to clear the drive and tower lattice. That view keeps the actual carrier foot and receiving-cart bed visible through lowering at344–348 and the subsequent four-second landing hold, instead of leaving contact beneath the playback controls in the full rig shot. This transition must also clear the completed tower near frustum.

Verification: focused main-film clock/identity/cut tests; exact preservation of earlier first-floor orbit samples; two-second camera-join continuity; real world orchestration and resource lifecycle; existing sampler/renderer regressions. Actual final production desktop/mobile browser checks must verify served asset/bundle identity, phase joins, reverse seeks, framing and unchanged triangle/draw-call limits. This insertion does not establish hardware strength, rope inventory capacity, equipment erection/removal or the omitted higher relay and final installation.

Final verification: the complete pre-culling integration baseline passes953/953 tests in176files; the affected camera/film subset passes35/35. Subsequent geometry-preserving culling passes21/21 focused tests. Final typecheck/build pass; the eight recorded integration modules are unchanged from the full-test baseline. Actual served production bundle and relay hashes match dist/public. Fifteen production Chromium viewport phase probes per desktop/mobile pass unchanged limits: desktop343524 triangles/118calls and mobile293212/106. Reverse screenshots are pixel-exact; Play advances and Pause holds, with zero browser errors. The0.001 slider step quantizes requested screenshot times; exact subframe joins are established by engine tests, while actual neighboring slider steps are a separate browser check. This does not establish real-device frame rate or whole-film optical/mechanical correctness.

Dense Paris source triangles use13m spatial cells for the extended final mobile orbit (previously26m), with35m bounded sliver merging. Partitioning preserves complete source triangles, normals, vertex colors, UVs and facade metadata; it does not remove buildings, actors or geometry. Sparse silhouette cells remain. The city still uses one multi-draw material batch. Verify source-corner/metadata preservation and the actual final390×844 production frame against the unchanged300,000-triangle limit; a finer grid alone is not evidence of GPU budget success.

The rebuilt Eiffel environment renders the exact static tree crown/trunk instance arrays through two individually culled BatchedMeshes. Each batch stores its one original geometry and every original matrix/color; original InstancedMeshes remain as hidden source records. Counts, materials, shadows, positions, tree sizes and crowd/city density stay unchanged. Packed batch resources have independent disposal. Verify actual source arrays and per-instance transforms/colors, then measure the same final mobile frame against300k/150 without relaxing either limit.

The13m/35m partition has1558 measured cells, preserving the unchanged1600-cell bookkeeping bound and all202312 source triangles after the previously accepted basin paving edit. Source asset and facade metadata tests pass9/9. Actual GPU acceptance remains a separate gate; neither count nor mobile GPU threshold may be relaxed.

The prepared, basin-excavated Eiffel terrain is partitioned into at most64 UV-centroid cells (8×8), using complete triangles without new cuts. Every vertex attribute, triangle winding and support surface remains exact. One BatchedMesh shares the original terrain material; each cell has an identity instance matrix and individual frustum culling. Packed batch resources and original prepared geometry retain distinct disposal ownership. Terrain density, horizon extent, basin excavation and material filtering do not change. Verify full triangle/attribute multisets and actual support rays before the mobile GPU gate.

The existing excavation output has position, normal and color attributes but no UV. For this output only, recover the original plane coordinates by inverting the existing450m/15× outer stretch and normalizing by the original diameter. These coordinates select cells only; no UV attribute or shader input is added. The actual prepared terrain passes ordered triangle/all-attribute multiset equality, exact union bounds,42 matched source/packed support rays and separate disposal checks.
