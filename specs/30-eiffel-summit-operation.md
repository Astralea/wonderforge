# Eiffel summit operation candidate

The next candidate composes the two existing112s terrace cargo lifts and the
70s supported gin-pole climb and25s empty-tackle return into one319s sequence. It remains
separate from the production film until the earlier ground-to-terrace relay,
operation transitions, receiving release, controls and full physical admission
are resolved. A successful export alone does not admit it.

The parent authors the editable Blender model through the installed Blender
MCP, with an initial read-only desktop probe and an isolated source scene.
Save it under artifacts/eiffel-summit-operation-2026-09-08; retain previous
candidate sources and evidence. Engine/render/test subtasks may be delegated.

Replace the mirrored lower cargo drive with a frame supported by seated m073.
Keep its rear crosshead at Z+.70 and western return outside X-1.26. The four
lower guide centres are [.25,303.14,.58], [.19,303.32,.64],
[-1.18,303.38,.56], [-1.26,303.46,-.26]. Cargo riser is X-1.26,Z-.34;
climbing rope remains X-1.10,Z+.34. Test actual exported geometry against the
full80s worker-access trajectory, including released extremities.

The matching head annulus uses side distance hypot(.36,.34), pitch radius
sqrt(.36²+.34²-.06²), a .30m offset heel and5.70m jib. The fixed upper
crosspin uses opposed stubs around a genuinely open vertical swivel bearing.

A sliding keyed cargo drum keeps the live takeoff at X=.25. Its winding and
24-turn closed traverse groove use the same deterministic displacement law;
retained cam follower and positive mechanical drive must be visible. Derive
rope inventory, takeoff tangent and shaft rotation together rather than
displaying static full-capacity rings. Fixed bearings remain stationary.

Luffing needs a separate restraining tackle and drive. Candidate yaw-local
drum[-.15,.60,0], topguide[-.20,1.13,0], becket[.16,1.0,0], and boom-local
movingblock[5.25,.34,0] define an explicit two-fall route. Export the block's
positive boom connection. Unfinished control or winding work must remain
identified as such, rather than being inferred from mathematical poses.

The second stock attachment is explicitly omitted in this candidate. A smooth
empty-block return does not prove cart arrival, sling attachment or unloading.
Expose this unresolved boundary in the preview. The present narrow-angle
diamond-cam crossings also lack demonstrated positive branch selection; sampled
follower/groove clearance must not be promoted to mechanical drive admission.

Long rope spans retain their physical mesh and a depth-tested one-pixel
centreline for distant viewing. The display line avoids broken subpixel spans
without altering the physical radius, route or inventory. It must remain
occluded by solids and must not write a false wide depth silhouette.

Run focused geometry/conservation/contact regressions, then the required full
test/typecheck/build suite and real desktop/mobile browser QA. Preserve the
identity and hash of the exact source used by each check. Production admission
also needs connected phase boundaries, which isolated seek screenshots cannot
establish. Strength and exact historical apparatus are not certified here.
