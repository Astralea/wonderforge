# Eiffel reference-led modeling and sequence verification

Owner request2026-09-12: keep real loader progress but animate its fill boundary
while progress stalls; inspect dozens of short-film frames for causal order;
implement the justified modeling improvements from the reference survey.

Owner clarification2026-09-12: desktop is primary. Keep the completed mobile
accommodations lightweight; further modeling and interaction work is judged
primarily on the desktop experience.

## Modeling contract

Work remains Eiffel-only, with its180-second default. The tower's typed
construction graph and final-size physical source solids remain authoritative.
Keep all other scenes and internal detailed-film mechanics functional.

Use the existing 1889 photograph/print reference boards and fresh primary-source
research for recognizable exhibition architecture and Paris roof detail.
Coordinates and hidden elevations remain authored/compressed interpretations.
Retain footprints, terrain support, court circulation and logistics keepouts.
Author new static geometry through Blender in a new scene and candidate output
folder; preserve current assets and user's active Blender scene/file.

Prioritize actual short-film views: articulated exhibition roof glazing/ridges,
readable foreground town-house dormers and chimney groups, then material
separation between limestone, zinc/slate, painted metal, glass, paving and soil.
Keep new detail within the existing spatial batching. Avoid new transparent
passes, unnecessary draw calls or microdetail that shimmers at film distance.
Desktop/mobile sampled limits remain200calls/450000tri and150calls/300000tri.
Dense review may reveal pre-existing overruns; distinguish them and resolve
introduced regressions rather than changing the budget to pass.

Wide short-film views use the existing major-iron shadow tier on desktop as
well as mobile once the camera is at least400m from the tower center. Keep
every visible source solid and the deck, masonry, roof and major iron shadows;
restore the full shadow source for close construction views and the desktop
detailed edition. This removes distant fine-lattice shadow submissions, not
visible tower structure. Verify toggling and reverse seeks preserve matrices.

## Evidence and sequence contract

Capture at least40 actual short-film frames per desktop/mobile viewport,
including before/after every chapter boundary. Save timestamps, mapped source
clock, production height/phase and render diagnostics with the images. Inspect
causal order, supports, attachment/release, framing and caption correspondence.
Fix demonstrated sequence defects; an edit cut must not make erected structure
unbuild or installed parts disappear. Test corrected boundaries and reverse
seeks against the actual state graph and geometry. Record authored temporal
compression separately from historically verified claims.

Verify a stalled real loader at more than one wall-clock time: same percentage,
different visible waterline, no clock advance until readiness. The moving crest
must be a viewBox-aligned SVG graphics group with compositor `translate3d`,
not a clip-path child or a child of `backdrop-filter`. At 0% the crest plus a
short gold band must remain readable on the lower legs. Reduced-motion disables
ambient wave movement while retaining progress updates and accessible values.

## Rendering and source preservation

Material variation may add shader attributes but must preserve source triangle
positions, normals, UVs and batching/culling. Imported roof/frontage additions
need measurable source/output bounds, support and triangle count evidence.
Do not substitute a new renderer, copied reference art or bloom for modeling.

Eiffel's materials receive one low-resolution filtered sky/ground reflection
environment, derived from its own typed daylight palette. It is baked once,
not rendered per frame, with intensity falling at dusk/night. It supplies the
missing broad reflections for zinc and glass; it is not a reflection of actual
nearby geometry. Other wonders keep their environment settings. Dispose the
bake and source texture explicitly; measure startup and composed-frame cost.


## Deferred procedural environment fallback

For rebuilt Eiffel, create actual terrain, quays and quay trees before asset
readiness. Defer procedural city blocks, their window/roof scatter, stocks,
forges and static barges until either the reviewed Paris city or life asset
fails. Keep their original shared random sequence through a continuation after
quay-tree placement, so fallback transforms stay identical in either failure
order. If only one load fails, hide the fallback content already supplied by
the successful asset. Construct the legacy Ecole and Champ paths only on city
failure. The legacy environment remains eager and visually unchanged.

Disposal cancels this deferred allocation; a later failed fetch must not create
new geometry or materials. Verify zero eager fallback matrix writes/resources
under deferred fetch, unchanged tree and fallback poses, actual rejected fetch
recovery, both completion orders and disposal-before-failure. This removes
unused CPU/resource preparation; it is not a claimed download-time improvement
or permission to omit machinery used by the short film or reverse seeking.

## Short-film chronology correction

The pre-change57-frame desktop/mobile audit finds no deliberate reversed source
order, but the tower grows from about22m to292m in the82–90second passage and
then remains at that height for roughly60seconds. Allocate82–112seconds to that
actual structural rise, and shorten the near-complete relay interval to112–150.
The relay approach must arrive close for its physical receiver/landing phase,
with the payload, platform and lifting support visible; a ground-level glimpse
cannot stand in for a platform handoff. Preserve source geometry, poses and
attachment order. Keep all180seconds and test monotone/invertible source mapping,
reverse seeks, narration window agreement and completed-kit camera clearance.
Save old/new dense seated-part/height samples, distinguish numerical endpoint
classification from real disappearance, and recapture actual rendered frames.

At completed insertion boundaries, pin the inverse production clock to the exact
completed insertion's production coordinate. This prevents floating-point
subtraction/inversion from briefly returning below an already-completed member
endpoint. This is a mathematical interval bound, not an epsilon that admits
early seating, and applies to both Eiffel editions.

## Opt-in render cost diagnostics

The local query flag `?renderCosts=1` may expose per-mesh main/shadow submission
counts on the canvas `data-render-costs` attribute. Measure actual renderer.info
counter deltas around Mesh render/shadow callbacks; preserve original callback
receivers and arguments and restore them after every frame, including errors.
Include total frame counts and an unattributed remainder for postprocessing or
non-Mesh submissions. This flag is off by default and must not change geometry,
visibility, shadow selection or culling. Its instrumentation adds CPU overhead,
so use it to identify triangle/call contributors, not to claim frame-rate parity.

## Cinematic joint framing

The short film's64–74second joint view retains its checked eye position and
uses a tighter lens fitted to the existing splice, worker and bearing deck.
Fit horizontal/vertical margins for both desktop and mobile. Keep source clocks,
poses, beam geometry, caption windows and detailed camera unchanged. Wide side
azimuth candidates fail completed-tower near-plane clearance, and pitch changes
increase sampled equipment/iron occlusion, so neither is admitted. This improves
work scale and excludes surrounding clutter; it does not remove every existing
beam overlap. Test full focus-envelope projection and all approach/exit
clearance, then review actual frames before claiming the sightline is resolved.

## Short-film equipment shadow detail

Dense mobile sampling exposed wide shots exceeding the existing300k triangle
budget. Actual per-mesh counters at150seconds identify34,728 shadow triangles
from material batches containing only fine first-floor/second-floor equipment.
At cinematic camera distance at least180m from the existing tower shadow center
[0,156,0], omit shadows only for those entirely fine batches. Keep a whole batch
if any exported source member's two smallest transport dimensions have a product
at least0.08m², matching the tower major-chord criterion. Always retain the actual
carried member, even when thinner. Mixed support/deck batches retain every caster.

All visible solids, batch counts, attributes, transforms, shadow reception,
ropes, onward work and the separate steam-drive rule remain unchanged. Closer
shots and every Detailed view restore full source equipment shadows, including
when the view was chosen before the assets finished loading. Test actual asset
classification, unchanged matrices and forward/reverse restoration; verify the
composed mobile budget again in the browser. These are shadow submissions only,
not an alteration to physical construction order or a new budget allowance.

The equipment threshold differs from the tower's400m threshold: the120-second
approach remains a wide view at roughly190m, before the full receiver close-up.
The earlier400m equipment gate restored tiny casters during that approach and
exceeded the mobile limit. Preserve full shadows in the actual122–130s close-up
and verify both sides of this threshold with actual browser frames.
