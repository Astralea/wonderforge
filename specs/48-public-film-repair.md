# Four On site film repair — 2026-09-20

Owner accepted the current public-release audit and authorized repair. This
spec scopes that repair; it does not authorize deployment or a social post.
Preserve the four published IDs, illustrated title mark, Giza homepage,
existing music/narration, deterministic clocks and Eiffel sunrise ending.

## Construction contracts

- Giza's visible ramp, active load and crew use one pure support surface.
  Contact tests use actual renderer transforms/geometry, including the active
  high-course samples identified by the audit. Each worker samples support at
  their own position; the load and runners agree with the ramp slope. Static
  core fill is not rebuilt while its construction state is unchanged.
- Stonehenge uprights rotate around the actual local butt transformed into
  the authored pit, with rope attachment derived from that same rigid pose.
  Lintel cribs are assembled from grounded fixed-size members and remain
  load-bearing through the guided traverse until both joints support the load.
- Colosseum haul subsegments join continuously in position and heading.
  Wagons support the load without intersecting it. Timber crane station
  dimensions are fixed; hook travel comes from a reachable rigid rig. Scaffold
  strike removes final-size members progressively through the last storeys,
  with no bulk disappearance at 98%.

## Presentation

- During construction, chapter text has priority over quotations. Quotes may
  wait until the reveal; the complete attributed quote remains available in
  the information panel at all times. Portrait titles, captions, index and
  transport must leave the active work visible and never overlap each other.
  Compact chapter disclosure applies to all films on narrow screens.
- Stonehenge retains its solstice sun and long shadows but the stones and
  raising mechanism remain readable. Giza receives a closer operation view
  without changing human/stone scale; preserve the separated ensemble reveal.
- Colosseum terrain and sky blend without a straight horizon seam. Preserve
  the dense Rome neighbourhoods and proper interior bowl. Reduce distant
  geometry/shadow cost rather than removing the authored surrounding city.
- Eiffel closeups retain an unobstructed load/cable/receiving-joint sightline;
  upper iron remains legible against the sky. Preserve current film timing,
  rigid member identities and mechanical clocks.

## Delivery and lifecycle

- Owner addition: cinematic arrival fill runs for at least 600 ms from its first paint.
  Display the lesser of measured readiness and the 600 ms presentation ramp;
  never show 100% before assets are ready. Slow loads retain their measured
  progress. Show the complete 100% silhouette for 100 ms before revealing the
  scene; the film clock stays gated until then. Errors bypass the minimum.
  Homepage ambient shows no loading animation or progress overlay and has no
  minimum presentation delay. Its title remains available during preparation.

- Reduce Eiffel runtime manifest/Paris download cost without losing structural
  identities, typed poses or readiness guarantees. Keep original authoring
  assets available and test optimized runtime assets against their sources.
  Regenerate the delivery assets before every production build. The city
  reader accepts both raw gzip and fetch-decoded GLB responses.
- Unversioned model, manifest, audio and brand URLs revalidate after a new
  release, so updated code cannot silently combine with day-old assets.
- Reduced-motion playback is event-driven and does not spin an unchanged RAF.
  Every owned instance buffer and postprocessing pass is disposed.
- Seeking to the endpoint produces a replayable complete state; reverse seek
  returns to paused. Existing keyboard and speed behavior remains usable.
- Supply explicit share title/description/image metadata from existing
  project imagery; do not publish or submit a post during this repair.

## Verification

Extend meaningful tests for transformed contacts, internal route boundaries,
fixed rig dimensions, progressive scaffold removal, UI disclosure, endpoint
state, optimized assets and disposal. Run the full test suite, typecheck and
production build. Compare actual production-rendered desktop 1440×900 and
portrait 390×844 states against the audit, plus 320×844 Eiffel framing.
Record current renderer counts and normal-speed operation samples, including
Giza ~50%, Stonehenge .083/.21055/.2135, Colosseum internal haul/98% strike,
and Eiffel 34–42 s / 104–124 s. Separate browser viewport results from physical
phone results and identify unavailable device/audio verification honestly.

### Eiffel load-view camera trial

For the ground lift and joint delivery, raise the local working view from 5° to
16° so the payload and rope can read above the surrounding lattice. The ground
cart approach and vertical hoist retain 5°; elevation blends during pilot
seconds 26–30 as the raised load turns, preserving its view past the falsework. Preserve
42° FOV, the target path, the continuous global azimuth, all physical/editorial
timing and the 25° detailed fastening view. Recompute the existing bounds fit
at the new pitch. Accept only after full payload/rig framing and 5 m near-frustum
clearance checks plus desktop/portrait comparison at 12% and 22%; the existing
payload centreline was already physically visible, so this is a composition
repair rather than a change to the construction mechanism.
